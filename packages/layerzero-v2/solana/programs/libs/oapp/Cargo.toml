[package]
name = "oapp"
version = "0.2.0"
edition = "2021"

[features]
idl-build = ["anchor-lang/idl-build", 'endpoint-interface/idl-build']

[dependencies]
anchor-lang = "0.31.1"
endpoint-interface = { path = "../../programs/endpoint-interface" }
